import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Pricing - Product Tour: Interactive Product Tours & User Onboarding Software",
};

export default function PricingLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
